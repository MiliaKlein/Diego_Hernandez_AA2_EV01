package com.example;

public class Main {
    public static void main(String[] args) {
        /*
         * Se invocan los métodos y hacen las pruebas
         */

        // Conexión
        // dbConnection jdbc = new dbConnection();
        // jdbc.connect();

        // Operaciones CRUD
        // crud crudMethods = new crud();
        // Create
        // crud.createStatement("INSERT INTO `user` (`name`, `access_lvl`, `user_type`) VALUES (?, ?, ?)");
        // Read
        // crud.readStatement();
        // Update
        // crud.updateStatement("UPDATE `user` SET `name` = ?, `access_lvl` = ?, `user_type` = ? WHERE `user`.`id` = ?");
        // Delete
        // crud.deleteStatement("DELETE FROM `user` WHERE `user`.`id` = ?");
    }  
}